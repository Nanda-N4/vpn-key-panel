<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Key • <%= panelConfig.brandName %></title>
  <link rel="stylesheet" href="/public/style.css"/>
</head>
<body>
  <div class="bg"></div>

  <header class="topbar">
    <a class="brand" href="/">
      <span class="brandMark">⚡</span>
      <span class="brandText"><%= panelConfig.brandName %></span>
    </a>

    <a class="tinyLink" href="<%= panelConfig.telegramAdminUrl %>" target="_blank" rel="noopener">
      <span class="tgDot">TG</span> Support
    </a>
  </header>

  <main class="wrap">
    <section class="card">
      <div class="rowBetween">
        <div>
          <h2 class="h2"><%= row.key_type %> • <%= row.region_flag %> <%= row.region_name %></h2>
          <div class="metaLine">
            <span>GB: <b><%= row.gb_limit %></b></span>
            <span class="dot">•</span>
            <span>Exp: <b><%= row.expire_date %></b></span>
            <span class="dot">•</span>
            <span class="badge <%= status === 'ACTIVE' ? 'ok' : 'bad' %>"><%= status %></span>
          </div>
        </div>

        <a class="btn btnGhost" href="/">← Back</a>
      </div>

      <div class="divider"></div>

      <div class="field">
        <div class="label">Key</div>
        <div class="copyRow">
          <input id="vpnKey" class="input mono" readonly value="<%= row.key_string %>"/>
          <button class="btn btnPrimary" type="button" onclick="copyKey()">Copy</button>
        </div>
      </div>

      <div class="divider"></div>

      <h3 class="h3">Download Apps</h3>
      <div class="appsGrid">
        <a class="appCard" href="<%= apps.windows %>" target="_blank" rel="noopener">Windows</a>
        <a class="appCard" href="<%= apps.mac %>" target="_blank" rel="noopener">Mac</a>
        <a class="appCard" href="<%= apps.android %>" target="_blank" rel="noopener">Android</a>
        <a class="appCard" href="<%= apps.ios %>" target="_blank" rel="noopener">iPhone/iPad</a>
      </div>

      <div class="divider"></div>

      <div class="callout">
        <div class="callTitle">Buy / Support</div>
        <div class="callSub">ဝယ်ယူ/အကူအညီလိုပါက Telegram မှ ဆက်သွယ်ပါ</div>
        <div class="btnRow">
          <a class="btn btnPrimary" href="<%= panelConfig.telegramAdminUrl %>" target="_blank" rel="noopener">
            <span class="tgDot">TG</span> <%= panelConfig.telegramAdminText %>
          </a>
          <a class="btn btnGhost" href="<%= panelConfig.telegramChannelUrl %>" target="_blank" rel="noopener">
            <%= panelConfig.telegramChannelText %>
          </a>
        </div>
      </div>
    </section>

    <footer class="footer">
      <span>© <%= new Date().getFullYear() %> <%= panelConfig.brandName %></span>
      <span class="dot">•</span>
      <a href="<%= panelConfig.telegramChannelUrl %>" target="_blank" rel="noopener">
        <%= panelConfig.telegramChannelText %>
      </a>
    </footer>

    <div id="toast" class="toast">Copied ✅</div>
  </main>

  <script>
    async function copyKey(){
      const el = document.getElementById("vpnKey");
      try{
        await navigator.clipboard.writeText(el.value);
      }catch(e){
        el.focus(); el.select();
        document.execCommand("copy");
      }
      const t = document.getElementById("toast");
      t.classList.add("show");
      setTimeout(()=>t.classList.remove("show"),1200);
    }
  </script>
</body>
</html>
