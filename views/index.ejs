<div class="stack-lg">
  <section class="card hero">
    <div class="stack-md">
      <div class="kicker">VPN Key Portal</div>
      <h1 class="title">Key ကို ရွေး → Copy → Paste → Connect</h1>

      <p class="muted">
        ဤ Website မှာ All SIM WIFI အတွက် VPN Key တွေကို လွယ်လွယ်ကူကူ ရွေးပြီး Copy → သင့် App ထဲ Paste → Connect လုပ်နိုင်ပါတယ်။
      </p>

      <div class="hero-actions">
        <% if (panelConfig?.telegramAdminUrl && panelConfig.telegramAdminUrl !== "#") { %>
          <a class="btn btn-soft" href="<%= panelConfig.telegramAdminUrl %>" target="_blank" rel="noopener">
            💬 <span><%= panelConfig.telegramAdminText || "Contact Admin" %></span>
          </a>
        <% } %>
      </div>

      <div class="steps card-soft">
        <div>1) Key ရွေး</div>
        <div>2) Copy</div>
        <div>3) App ထဲ Paste</div>
        <div>4) Connect</div>
      </div>
    </div>
  </section>

  <section class="card">
    <form class="filter" method="GET" action="/">
      <input
        class="input"
        type="text"
        name="q"
        value="<%= query?.q || "" %>"
        placeholder="Search location... (e.g. Singapore, Japan)"
        autocomplete="off"
      />

      <select class="select" name="type">
        <option value="">All Types</option>
        <% (types || []).forEach(function(t){ %>
          <option value="<%= t %>" <%= (query?.type === t) ? "selected" : "" %>><%= t %></option>
        <% }) %>
      </select>

      <select class="select" name="status">
        <option value="">All Status</option>
        <% (statuses || []).forEach(function(s){ %>
          <option value="<%= s %>" <%= (String(query?.status || "").toUpperCase() === s) ? "selected" : "" %>><%= s %></option>
        <% }) %>
      </select>

      <button class="btn btn-pill" type="submit">🔎 Filter</button>
    </form>
  </section>

  <section class="stack-md">
    <% if (!items || items.length === 0) { %>
      <div class="card empty">
        <div class="muted">Result မရှိသေးပါ။ Search / Filter ပြန်စစ်ကြည့်ပါ။</div>
      </div>
    <% } %>

    <% (items || []).forEach(function(x){ %>
      <a class="card key-row" href="<%= x.link %>">
        <div class="key-left">
          <div class="key-type"><%= x.type %></div>

          <div class="key-meta">
            <span class="flag"><%= x.region_flag || "🌐" %></span>
            <span class="region"><%= x.region_name %></span>
          </div>

          <div class="key-sub">
            <span>GB <%= x.gb_limit || 0 %></span>
            <span class="dot">•</span>
            <span>Expire <%= x.expire_date || "-" %></span>
          </div>
        </div>

        <div class="key-right">
          <span class="badge <%= (x.statusComputed || 'ACTIVE').toLowerCase() %>">
            <%= x.statusComputed || "ACTIVE" %>
          </span>
          <span class="chev">Tap to open →</span>
        </div>
      </a>
    <% }) %>
  </section>

  <section class="card footer-card">
    <div class="stack-sm">
      <div class="muted">အသုံးပြုနည်း (အတိုချုပ်)</div>
      <div>1) Key ကို Copy နှိပ်ပါ</div>
      <div>2) သင့် App ထဲ Import/Add → Paste</div>
      <div>3) Connect လုပ်ပြီး သုံးပါ</div>

      <div class="row">
        <% if (panelConfig?.telegramAdminUrl && panelConfig.telegramAdminUrl !== "#") { %>
          <a class="btn btn-soft" href="<%= panelConfig.telegramAdminUrl %>" target="_blank" rel="noopener">🧑‍💻 Help / Buy Key</a>
        <% } %>
        <% if (panelConfig?.telegramChannelUrl && panelConfig.telegramChannelUrl !== "#") { %>
          <a class="btn btn-soft" href="<%= panelConfig.telegramChannelUrl %>" target="_blank" rel="noopener">📣 Updates</a>
        <% } %>
      </div>
    </div>
  </section>
</div>
