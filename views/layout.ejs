<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="color-scheme" content="light dark" />
  <title><%= panelConfig?.brandName || "VPN KEY" %></title>

  <!-- CSS Architecture -->
  <link rel="stylesheet" href="/public/00-tokens.css" />
  <link rel="stylesheet" href="/public/01-base.css" />
  <link rel="stylesheet" href="/public/02-layout.css" />
  <link rel="stylesheet" href="/public/03-components.css" />
  <link rel="stylesheet" href="/public/04-pages.css" />
  <link rel="stylesheet" href="/public/05-utilities.css" />
</head>

<body>
  <div class="bg-aurora" aria-hidden="true"></div>

  <div class="app-shell">
    <!-- Header -->
    <header class="app-header">
      <div class="container header-inner">
        <div class="brand">
          <span class="brand-icon" aria-hidden="true">⚡</span>
          <div class="brand-text">
            <div class="brand-title"><%= panelConfig?.brandName || "VPN KEY" %></div>
            <div class="brand-sub">Fast • Simple • Secure</div>
          </div>
        </div>

        <div class="header-actions">
          <% if (panelConfig && panelConfig.telegramChannelUrl && panelConfig.telegramChannelUrl !== "#") { %>
            <a class="btn btn-pill" href="<%= panelConfig.telegramChannelUrl %>" target="_blank" rel="noopener">
              ✈️ <%= panelConfig.telegramChannelText || "Channel" %>
            </a>
          <% } %>
        </div>
      </div>
    </header>

    <!-- Main -->
    <main class="container page-main">
      <%- body %>
    </main>

    <!-- Footer -->
    <footer class="container app-footer">
      <div class="footer-row">
        <div>© <%= new Date().getFullYear() %> <%= panelConfig?.brandName || "VPN KEY" %></div>

        <% if (panelConfig && panelConfig.telegramAdminUrl && panelConfig.telegramAdminUrl !== "#") { %>
          <a class="footer-link" href="<%= panelConfig.telegramAdminUrl %>" target="_blank" rel="noopener">
            Support: <%= panelConfig.telegramAdminText || "Contact Admin" %>
          </a>
        <% } %>
      </div>
    </footer>
  </div>

  <!-- Announcement Modal -->
  <% if (panelConfig && panelConfig.announceHtml) { %>
    <div
      id="announceModal"
      class="modal-overlay is-hidden"
      data-version="<%= panelConfig.announceVersion || '' %>"
      role="dialog"
      aria-modal="true"
      aria-labelledby="announceTitle"
    >
      <div class="modal-card">
        <div class="modal-head">
          <div class="modal-head-left">
            <span class="badge">📣 Announcement</span>
          </div>

          <!-- X close -->
          <button type="button" class="icon-btn" data-close-modal aria-label="Close">✕</button>
        </div>

        <div class="modal-body">
          <div id="announceTitle" class="modal-title">Update</div>
          <div class="modal-content">
            <%- panelConfig.announceHtml %>
          </div>
        </div>

        <div class="modal-actions">
          <!-- OK close -->
          <button type="button" class="btn btn-primary" data-close-modal>OK</button>
        </div>
      </div>
    </div>
  <% } %>

  <script defer src="/public/06-app.js"></script>
</body>
</html>
<script>
  async function copyKey(btn){
    const text = btn.getAttribute('data-copy') || '';
    try{
      await navigator.clipboard.writeText(text);
      const old = btn.innerHTML;
      btn.innerHTML = '✅ Copied';
      setTimeout(()=> btn.innerHTML = old, 900);
    }catch(e){
      alert('Copy failed. Please tap open then copy.');
    }
  }
</script>
